# Secrets — NUNCA subir a git
.env
.env.local
.env.*.local

# Plantilla SÍ se sube (no tiene valores reales)
!.env.example

# Dependencias / plataforma
node_modules/
.vercel/

# Sistema operativo
.DS_Store
Thumbs.db
.vercel
.env*

# Worktrees temporales de agentes (Claude Code) — jamás versionar
.claude/worktrees/

# Imágenes sueltas de pruebas en la raíz (se colaron una vez, no vuelven)
/*.jpeg
/*.jpg

# Build (Vite)
node_modules/
dist/
*.tsbuildinfo

# Scratchpad de sesión (tooling temporal: generador del demo, capturas, etc.)
scratchpad/
output/
test-results/
.codex-vite-preview*.log
